{
  "name": "kanban-backend",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run build --prefix functions && concurrently \"npm:build:watch --prefix functions\" \"cross-env NODE_ENV=development firebase emulators:start --only functions,firestore,auth --inspect-functions --export-on-exit=./.firebase/emulator-data --import=./.firebase/emulator-data\"",
    "test:env": "cross-env NODE_ENV=development node -e \"console.log(process.env.NODE_ENV)\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
